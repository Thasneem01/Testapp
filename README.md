# Minimal Angular CRUD

This is a minimal Angular app demonstrating basic in-memory CRUD operations.

Prerequisites:
- Node.js and npm installed

Install and run (PowerShell):

```powershell
npm install
npx ng serve --open
```

The app uses a simple in-memory `ItemService` for create/read/update/delete.